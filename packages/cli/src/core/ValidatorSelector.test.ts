import { ValidatorSelector } from './ValidatorSelector'

describe(ValidatorSelector.name, () => {
  it('should be an abstract class', () => {
    expect(typeof ValidatorSelector).toBe('function')
  })
})
